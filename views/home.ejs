<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width= , initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>EJS</h1>
    <%if (locals.todo) { %>
        <%= todo.title %>
    <% } %>


    <%if (locals.id) { %>
        <%= id %>
    <% } %>
    
</body>
</html>